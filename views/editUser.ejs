<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuario</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Editar Usuario</h1>
    <form action="/users/<%= user._id %>/edit" method="POST">
        <label for="username">Nuevo nombre de usuario:</label>
        <input type="text" id="username" name="username" value="<%= user.username %>" required>
        <label for="password">Nueva contraseña:</label>
        <input type="password" id="password" name="password">
        <label for="privilegios">Privilegios:</label>
        <input type="number" id="privilegios" name="privilegios" value="<%= user.privilegios %>">
        <label for="autorizacion">Autorizacion:</label>
        <input type="text" id="autorizacion" name="autorizacion" value="<%= user.autorizacion %>">
        <button type="submit">Guardar cambios</button>
    </form>
    <a href="/users">Cancelar</a>
</body>
</html>
